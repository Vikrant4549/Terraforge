# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.backup

# Variables (if you add later)
terraform.tfvars

# OS
.DS_Store